{
  "private": true,
  "workspaces": {
    "packages": [
      "apps/*",
      "packages/*"
    ]
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run check",
    "lint:fix": "turbo run fix"
  },
  "packageManager": "pnpm@8.6.0",
  "devDependencies": {
    "rome": "^12.1.3",
    "turbo": "^1.10.1"
  }
}